{
  "version": "0.2.0",
  "configurations": [
 
    {
      "name": "启动应用 (npm run start)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run start",
      "cwd": "${workspaceFolder}"
    }
  ]
}
