{
  "version": "0.2.0",
  "configurations": [
 
    {
      "name": "启动应用 (pnpm run start)",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm run start",
      "cwd": "${workspaceFolder}"
    }
  ]
}
